apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
data:
  spring.datasource.url: "jdbc:mysql://mysql-service:3306/demo_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC"
  server.port: "8080"
  spring.jpa.hibernate.ddl-auto: "update"
  spring.jpa.show-sql: "true"

